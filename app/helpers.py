# from flask_wtf import FlaskForm
# from wtforms import StringField, validators, SubmitField, PasswordField, EmailField

# class FormularioLogin(FlaskForm):
#     nome = StringField('Digite seu usuário',[validators.DataRequired() , validators.length(min=1, max=50)] )
#     senha = PasswordField('Digite sua senha', [validators.DataRequired(), validators.length(min=1, max=100)])
#     entrar = SubmitField('Entrar')


